let num1: number= 10;
let num2: number= 20;
let num3: number= 70;
let sum:string = "The sum of the numbers is: ";
console.log(`${sum} ${num1+num2+num3}`);

// md stands for material design. Ionic uses modes 
// to customize the look of components. Each platform 
// has a default mode, but this can be overridden through 
// the global config. ios uses ios mode, while android uses md, 
// the rest that doesnt fit uses md.